const cart = [];

const handleCart = (state = cart, action) => {
    const product = action.payload;
    switch (actio) {
        case value:
            
            break;
    
        default:
            break;
    }

}

export default handleCart;